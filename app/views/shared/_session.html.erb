<% if current_user %>
  <p><%= link_to current_user.name, user_path %>
    <%= "(Administrador)" if current_user.is_admin? %>
    <%= "(Moderador)" if !current_user.is_admin? && current_user.is_mod?%>
    | <%= link_to 'Salir', sign_out_path, method: :delete %></p>
<% else %>
  <p><%= link_to 'Iniciar sesión', sign_in_path %> |
    <%= link_to 'Crear cuenta', register_path %></p>
<% end %>
