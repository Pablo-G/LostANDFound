$("#lost_objects").html( $(
    "<%= escape_javascript render 'search_response' %>"
) );

var href = "<%= search_page_path params[:search], params[:page] %>"

window.history.pushState({page: href}, "", href);
