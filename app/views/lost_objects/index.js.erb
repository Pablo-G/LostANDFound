$("#lost_objects").html( $(
    "<%= escape_javascript render 'search_response' %>"
) );

window.history.pushState("", "",
                         "<%= search_page_path params[:search], params[:page] %>");
